import React, { Component } from 'react';

export default class Header extends Component {
  render() {
    return (
        <div className="login-header">
            <div className="login-navbar-button">Login</div>
        </div>
    );
  }
}